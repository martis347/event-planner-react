import React from "react";
import styled from "styled-components";

const StyledWrapper = styled.div`
  height: 220px;
`;

const SmallCalendar = () => {
  return <StyledWrapper>Small calendar</StyledWrapper>;
};

export default SmallCalendar;
